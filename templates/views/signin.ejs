<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signin</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    
    <div class="container">
        <div id="form_wraper">
         
            <form action="/signin" method="post" autocomplete="off">
                
                <% if (messages.error) { %>
                    <span><%=messages.error%> </span>
                <% } %>
                <input type="text" placeholder="Enter username" id="name" name="name">
                <p class="none username">username must be 1-20 character and should not start with number!</p>
                <input type="text" placeholder="Enter user email" id="email" name="email">
                <p class="none useremail">email number must be 1-14 and not miss @ and .com</p>
                <input type="tel" placeholder="Enter user number" id="mobile" name="mobile">
                <p class="none usermobile">mobile number must be 1-11 character should not start with string!</p>
                <button class="btn" type="submit">submit</button>
            </form>
        </div>
    </div>

<script src="js/app.js"></script>
</body>
</html>